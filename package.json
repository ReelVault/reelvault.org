{
	"name": "reelvault-docs",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vitepress dev docs",
		"build": "vitepress build docs && bun run scripts/generate-llms.ts",
		"preview": "vitepress preview docs"
	},
	"devDependencies": {
		"@types/node": "^26.6.2",
		"vitepress": "^1.6.4"
	}
}
